{
  "name": "integratie-landkaart",
  "version": "1.0.0",
  "description": "Datagedreven Integratie-landkaart voor de pilot B2B Portaal (TF-234)",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "tsx scripts/validate.ts",
    "generate:visual": "tsx scripts/generate-visual.ts",
    "generate": "npm run validate && npm run generate:visual",
    "validate:workato": "tsx scripts/validate-workato.ts"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "yaml": "^2.3.4",
    "zod": "^3.22.4"
  }
}
